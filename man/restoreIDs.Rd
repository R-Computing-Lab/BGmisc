% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalizeIDs.R
\name{restoreIDs}
\alias{restoreIDs}
\title{Restore Original IDs from a Normalized Pedigree}
\usage{
restoreIDs(ped, id_map = NULL, id_cols = NULL, verbose = FALSE)
}
\arguments{
\item{ped}{A data.frame that was previously processed by
\code{\link{normalizeIDs}}.}

\item{id_map}{Optional. An ID mapping data.frame (as produced by
\code{\link{idMap}}). If \code{NULL}, the function uses the
\code{"id_map"} attribute attached by \code{\link{normalizeIDs}}.}

\item{id_cols}{Optional. Character vector of columns to restore. If
\code{NULL}, uses the \code{"id_cols"} attribute.}

\item{verbose}{Logical.}
}
\value{
A data.frame with original ID values restored.
}
\description{
Reverses the mapping applied by \code{\link{normalizeIDs}}, converting
numeric surrogate IDs back to the original ID values and types.
}
\examples{
ped <- data.frame(
  ID = c("A", "B", "C"),
  momID = c(NA, NA, "A"),
  dadID = c(NA, NA, "B")
)
ped_num <- normalizeIDs(ped)
restoreIDs(ped_num)

}
\seealso{
\code{\link{normalizeIDs}}
}
