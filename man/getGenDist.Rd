% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ped2genDist.R
\name{getGenDist}
\alias{getGenDist}
\title{Compute the generational distance between two individuals}
\usage{
getGenDist(
  ped,
  id1,
  id2,
  method = c("rank", "path", "mrca_min", "mrca_max", "mrca_all"),
  personID = "ID",
  momID = "momID",
  dadID = "dadID",
  max_gen = 25L,
  ...
)
}
\arguments{
\item{ped}{A pedigree data frame.}

\item{id1, id2}{IDs of the two individuals to compare.}

\item{method}{Distance method. One of:
\describe{
  \item{\code{"rank"}}{Absolute difference in generation numbers from
    \code{\link{ped2gen}}. Founders = 1, their children = 2, etc.}
  \item{\code{"path"}}{Minimum number of parent-child steps between the
    two individuals through any shared ancestor (undirected path length).}
  \item{\code{"mrca_min"}}{Total steps via the most recent common ancestor
    (fewest combined steps to reach a shared ancestor).}
  \item{\code{"mrca_max"}}{Total steps via the most distant common ancestor
    (most combined steps to reach a shared ancestor).}
  \item{\code{"mrca_all"}}{Aggregated distance across all common ancestors}
}}

\item{personID}{Character. ID column name. Default \code{"ID"}.}

\item{momID}{Character. Mother ID column name. Default \code{"momID"}.}

\item{dadID}{Character. Father ID column name. Default \code{"dadID"}.}

\item{max_gen}{Integer. Maximum generations to traverse. Default \code{25}.}

\item{...}{Additional arguments passed to \code{\link{ped2com}}
(e.g. \code{adjacency_method}, \code{verbose}, \code{sparse}).}
}
\value{
A single numeric value, or \code{NA} if no genealogical path exists.
}
\description{
Returns a single numeric value representing how many generations apart two
individuals are, according to the chosen method.
}
\seealso{
\code{\link{ped2genDist}}, \code{\link{ped2genDistFocal}}
}
