% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/buildmxPedigrees.R
\name{.temporalFamilyGroupAlgebra}
\alias{.temporalFamilyGroupAlgebra}
\title{Build the temporal covariance algebra for one family group (internal)}
\usage{
.temporalFamilyGroupAlgebra(
  group_name,
  fsize,
  active,
  relmat_list,
  full_df_row,
  obs_ids,
  birth_year,
  H = NULL,
  use_exp_loadings = TRUE,
  condenseMatrixSlots = TRUE,
  time_point_max = NULL
)
}
\arguments{
\item{group_name}{Name of the family group.}

\item{fsize}{Family size (number of members), as computed by \code{\link{buildOneFamilyGroup}}.}

\item{active}{The filtered component table built by \code{\link{buildOneFamilyGroup}}
(one entry per relatedness matrix actually supplied), used for its \code{k} (loading
key) and \code{K} (loading-matrix name) fields.}

\item{relmat_list}{The list of already-built (and already condensed, if requested)
\code{"Symm"} mxMatrix objects for \code{active}, in the same order.}

\item{full_df_row}{A 1-row matrix/vector of observed data.}

\item{obs_ids}{Character vector of individual IDs, matching \code{full_df_row}.}

\item{birth_year}{Numeric vector of birth years, one per family member, matching \code{obs_ids}.}

\item{H}{Optional numeric matrix of historical moderators.}

\item{use_exp_loadings}{Logical. If TRUE, each component's loading is exponentiated.}

\item{condenseMatrixSlots}{Logical. If TRUE, condense the \code{Tpoly}/\code{H} mxMatrix objects.}

\item{time_point_max}{Integer degree of the polynomial birth-year basis.}
}
\value{
An OpenMx model for the specified family group.
}
\description{
Given a family's already-detected size, already-filtered component table, and
already-built relatedness mxMatrix objects (all shared with the static branch via
\code{\link{buildOneFamilyGroup}}), builds the time-varying covariance algebra: each
component's relatedness matrix is combined (via a Hadamard product) with a loading
matrix derived from a polynomial birth-year basis and, optionally, historical
moderators.
}
\keyword{internal}
