% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ped2genDist.R
\name{ped2genDist}
\alias{ped2genDist}
\title{Compute a full pairwise generational distance matrix}
\usage{
ped2genDist(
  ped,
  method = c("rank", "path", "mrca_min", "mrca_max", "mrca_all"),
  personID = "ID",
  momID = "momID",
  dadID = "dadID",
  max_gen = 25L,
  ...
)
}
\arguments{
\item{ped}{A pedigree data frame.}

\item{method}{Distance method; see \code{\link{getGenDist}}.}

\item{personID}{Character. ID column name. Default \code{"ID"}.}

\item{momID}{Character. Mother ID column name. Default \code{"momID"}.}

\item{dadID}{Character. Father ID column name. Default \code{"dadID"}.}

\item{max_gen}{Integer. Maximum generations to traverse. Default \code{25}.}

\item{...}{Additional arguments passed to \code{\link{ped2com}}
(e.g. \code{adjacency_method}, \code{verbose}, \code{sparse}).}
}
\value{
A numeric matrix with row and column names set to individual IDs.
}
\description{
Returns an n x n matrix of generational distances between all pairs of
individuals in \code{ped}.  Pairs with no genealogical path receive
\code{NA}.

Uses \code{\link{ped2com}} with \code{component = "distance"} to build the
ancestor distance matrix via the same adjacency construction and power
iteration used by all other pedigree components, then reduces it to pairwise
distances with vectorised \code{outer} + \code{pmin}/\code{pmax}.
}
\seealso{
\code{\link{getGenDist}}, \code{\link{ped2genDistFocal}}
}
