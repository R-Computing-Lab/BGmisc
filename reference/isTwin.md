# Determine isTwin Status

Determine isTwin Status

## Usage

``` r
isTwin(ped)
```

## Arguments

- ped:

  pedigree data frame

## Value

A logical vector indicating, for each row of `ped`, whether `twinID` is
non-`NA`.
